# Fetcher runtime dependencies
# Install: pip install -r requirements.txt

# API
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Config & validation (BaseSettings in pydantic 1.x)
pydantic>=1.10.0,<2.0.0

# Queue
celery[redis]>=5.3.0
redis>=4.5.0

# Storage
boto3>=1.28.0

# Observability
prometheus-client>=0.17.0

# HTTP client (webhooks, backpressure; [socks] for socks5:// in discover/enrich)
httpx[socks]>=0.24.0
PySocks>=1.7.1

# YouTube
yt-dlp>=2023.0.0
pytubefix>=9.5.0
# PO Token provider plugin (yt-dlp auto-detects it once the local bgutil server is up —
# см. Fetcher/scripts/setup_pot_provider.sh); требует yt-dlp>=2025.05.22
bgutil-ytdlp-pot-provider>=1.3.0

# Platform SDKs (TikTok, Instagram, Twitch)
TikTokApi>=6.0.0
playwright>=1.40.0
instaloader>=4.10
twitchAPI>=4.0.0

# Alembic (migrations)
alembic>=1.12.0
