# Copy this file to `.env` locally (the `.env` file is gitignored) and edit values as needed.
#
# --- Redis / Celery ---
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

# --- MinIO / S3-compatible storage ---
MINIO_ROOT_USER=trendflow
MINIO_ROOT_PASSWORD=trendflow123

# When running inside docker-compose network, use service name:
S3_ENDPOINT=http://minio:9000

# Bucket/prefix used by DataProcessor storage adapter
S3_BUCKET=trendflow
S3_PREFIX=trendflowml

# Standard AWS env var names (boto3-compatible)
AWS_ACCESS_KEY_ID=trendflow
AWS_SECRET_ACCESS_KEY=trendflow123
AWS_DEFAULT_REGION=us-east-1

# --- Triton (optional in PR-0; enabled via `docker compose --profile triton up`) ---
TRITON_HTTP_URL=http://triton:8000

# --- Models (no-network) ---
# ModelManager will resolve all model artifacts strictly from this directory.
# Recommended default for local dev: point to the repo `models/` directory.
DP_MODELS_ROOT=/home/ilya/Рабочий стол/TrendFlowML/DataProcessor/models

# --- ModelManager / local models (NO NETWORK) ---
# Must point to a local directory containing model artifacts (e.g. `visual/places365/...`).
# In docker image / compose, mount your artifacts into `/app/models`.
# For local dev in this repo, a convenient default is: <repo>/models
DP_MODELS_ROOT=/app/models


