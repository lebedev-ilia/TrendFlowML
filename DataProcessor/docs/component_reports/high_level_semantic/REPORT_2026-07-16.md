# REPORT: high_level_semantic — 2026-07-16

**Вердикт: ШТАМПОВАТЬ ✅ (авто-штамп при 100% PASS)**  
**Версия:** 2.0.2  
**Схема:** high_level_semantic_npz_v2  
**Дата:** 2026-07-16  

---

## Сводка

CPU-агрегатор. Зависимости: core_clip + cut_detection (жёсткие), emotion_face / audio / text (мягкие, graceful fallback).  
Все U1–U6 + C1–C4 PASS локально (frames_dir + deps есть).

---

## Изменения кода (Логика)

**Фикс: emotion_face → опциональная dep (graceful fallback)**
- `required_dependencies()`: убрана `emotion_face` из жёстких deps
- `_load_emotion_face()`: возвращает `None` если NPZ отсутствует или status=empty; логирует INFO (не RuntimeError)
- `process()`: `if "emotion" in self.feature_groups and emo_npz is not None:` (was: `if "emotion" in self.feature_groups:`)
- `events-блок`: `keyframes = emo_npz.get("keyframes") if emo_npz is not None else None`
- `ui.upstream`: добавлен флаг `emotion_face_present`

Аналогично аудио-deps и Triton-обходу в других компонентах.

---

## Гейты U1–U6

| Гейт | Результат | Детали |
|---|---|---|
| U1 validate rc=0 | **PASS** | 22/22 NPZ в storage batch-валидация OK (schema+struct+ranges); новые прогоны на 3 видео → rc=0 |
| U2 ось времени | **PASS** | struct-валидатор: frame_indices строго возраст., times_s неубывает на всех 22 NPZ |
| U3 finite/health | **PASS** | Inf=0%; NaN только audio (100% без upstream) и emo_* (0–100% в зависимости от лиц) — by design |
| U4 expected-empty | **PASS** | Тест 1: emotion_face.npz отсутствует → status=ok, emo_pr=0.0, emotion_face_present=False; Тест 2: синтетический emotion_face status=empty → то же |
| U5 golden | **PASS** | 2 прогона на `-0InsUQNwIQ`, max\|Δ\|=0.0 (numpy-детерминизм полный) |
| U6 разные длины | **PASS** | 3 видео: N=43 (S=1, E=0), N=65 (S=3, E=4), N=119 (S=6, E=9) → schema+struct+ranges OK |

---

## Критерии C1–C4 (сырые числа)

| Критерий | Измеренное | Порог | Статус |
|---|---|---|---|
| C1 clip_sim_prev present_ratio | 0.977 / 0.985 / 0.992 | ≥ 0.97 | **PASS** |
| C2 scene_embeddings L2-норм | 1.0000 (по всем 4 видео, max\|‖row‖₂ - 1\| < 0.001) | 1.0 ± 0.01 | **PASS** |
| C3 event_type_id | {1, 200} встречаются (210 — редко, при keyframes emotion_face); нет посторонних | ∈ {1,200,210} | **PASS** |
| C4 NaN by design | loudness/tempo: present_ratio=0.0 (нет upstream); emo_*: 0.0–0.892 (зависит от лиц) | задекларировано | **PASS** |

---

## Числа по видео (из локальных прогонов после фикса)

| video_id | N | S | E | clip_pr | emo_pr | schema |
|---|---:|---:|---:|---:|---:|---|
| -0InsUQNwIQ | 65 | 3 | 4 | 0.985 | 0.892 | OK |
| -4RHVBIikn8 | 119 | 6 | 9 | 0.992 | 0.782 | OK |
| -4WRepA-bss | 43 | 1 | 0 | 0.977 | 0.116 | OK |

---

## Оптимизация (тайминги)

Компонент CPU-only (numpy + dict-операции). На локальной машине:
- N=65, 3 сцены: ориентировочно < 1 с (без инициализации deps)
- Доминирует параллельная загрузка 6 upstream-артефактов (ThreadPoolExecutor)
- Для 200k: стоимость = только загрузка deps (core_clip, cut_detection, emotion_face) — сами вычисления пренебрежимы

---

## Инфра

- Валидатор: `validate_high_level_semantic.py --struct --ranges` (batch через `--results-base`)
- Раннер: используется инлайн через `HighLevelSemanticModule.run()` (отдельный CLI-раннер не требуется — достаточно существующего `main.py`)
- Сохранение: один NPZ `high_level_semantic/high_level_semantic.npz`

---

Авто-штамп: все U1–U6 + C1–C4 PASS, изменения кода минимальны и покрыты тестами U4.
