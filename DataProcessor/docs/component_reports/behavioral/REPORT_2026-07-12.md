# REPORT — behavioral (2026-07-12)

**Компонент:** VisualProcessor/modules/behavioral · schema `behavioral_npz_v1` · producer_version `2.0.1`
**Артефакт:** `behavioral/behavioral_features.npz`
**Deps:** Segmenter + core_face_landmarks (pose+hands+face-mesh). **Consumers:** Models (seq [N,F]+маска), аналитики (aggregated).
**Прогон:** под RunPod RTX 4000 Ada 20GB (netVol vuiq0iq3yf), CPU-only numpy. Набор B = 6 видео разной длины + smoke.
**Раннер:** `DataProcessor/scripts/run_behavioral_local.py` (Segmenter[shot_quality cfg]→OD→landmarks→behavioral→validate).

## Фиксы этой валидации (в git DataProcessor + на поде)
1. **body_lean_angle** (`behavior_analyzer.py:465-470`): убран насыщающий множитель `*5.0`, который загонял признак в
   константу `1.0` (для 720p `lean_raw≈0.42*5→clip=1.0`). Теперь варьируется с позой/дистанцией.
2. **producer_version** (`:763-764`): добавлен class attr `VERSION = "2.0.1"` (было `meta.producer_version="unknown"`).
3. **SCHEMA.md** §"Missing policy (иерархия опор — by design)": документирована иерархия NaN
   `landmarks_present ⊇ pose-опоры ⊇ mouth/face-mesh-опоры`.

## Набор B (сырьё)
| video | N | lp_true | analysis | status/reason | validator |
|---|---|---|---|---|---|
| 2XjrV6tfcbc | 34 | 34 | 720×1280 | empty / skipped_due_to_person_mask_no_person | rc=0 VALID |
| AGfPggrmw-E | 112 | 112 | ? | ok | rc=0 VALID |
| ItRcDFKFiSU | 272 | 272 | ? | ok (без pose-тира) | rc=0 VALID |
| 70_Id7JQot0 | 255 | 255 | ? | ok | rc=0 VALID |
| LogDeH7V6bM | 238 | 238 | 1080×1920 | empty / no_faces_in_video (только pose) | rc=0 VALID |
| QfzWnPhdg3g | 300 | 300 | 1920×1080 | ok | rc=0 VALID |
| smoke | 133 | 133 | ? | ok | rc=0 VALID |

Тайминги стадии behavioral (чистый numpy, CPU): 3.0–4.0 с/видео (N=34…300). Upstream: Segmenter ~19–22с, OD(YOLO11l) ~34–35с, landmarks(pose+hands+face) ~16–44с.

## Универсальные хард-гейты
- **U1 (валидатор rc=0):** schema VALID + `--struct` clean, rc=0 на всех 7. **PASS**
- **U2 (ось времени):** `frame_indices` строго возрастают ×7; `times_s`/`seq_timestamp_norm` 0 NaN; `seq_timestamp_norm∈[0,1]`. **PASS**
- **U3 (finite/health):** 0 Inf во всех `seq_*` ×7; `seq_gesture_prob_*∈[0,0.989]`; **сумма gesture-probs строго бимодальна** — 47.2% кадров =0.0 (num_hands=0, нет рук), 52.8% ≈1.0 (num_hands≥1, softmax), 0% «прочих». **PASS**
- **U4 (expected-empty):** синтетик (landmarks сдвинуты, 0 пересечений) → `lp_true=0/300`, весь основной `seq_*`=NaN, `aggregated` валидный dict(33 поля), `status=empty`, валидатор rc=0. Дополнительно 2 реальных «core-empty» видео (LogDeH7V6bM, 2XjrV6tfcbc) обрабатываются как empty без падения. **PASS**
- **U5 (golden):** повтор behavioral на тех же landmarks (QfzWnPhdg3g) → `max|Δ|=0.0` по всем `seq_*`, 0 расхождений NaN-паттерна, diff aggregated=0 полей. Чистый numpy → побайтово детерминирован. **PASS**
- **U6 (разные длины):** N=34…300 (видео ~6с…5мин+), все rc=0. **PASS**

## Критерии под компонент
- **C1 (NaN↔маска, ядро):** 23 core-поля (`num_hands`, `hands_visibility`, `hand_motion_energy`, `head_position_x/y_norm`,
  `head_motion_energy`, `head_stability`, `blink_flag`, `blink_rate_short`, `self_touch_flag`, `timestamp_norm`, 12×`gesture_prob_*`)
  = **0.00% NaN** на кадрах с `landmarks_present=True` (0 из 1344 present-кадров), 0 finite при `landmarks_present=False`. **PASS**
- **C2 (иерархия вторичных опор, by design):** pose-тир (`arm_openness`, `pose_expansion`, `body_lean_angle`, `balance_offset`,
  `shoulder_angle`, `shoulder_angle_velocity`) = ровно **57.22%** NaN (769/1344); mouth-тир (`mouth_*`, `speech_activity_proxy`)
  = ровно **56.18%** (755/1344). Проверка co-NaN: **0 кадров с частичным NaN** внутри тира (pose-поля NaN только все вместе,
  mouth-поля — все вместе) во всех 7 видео → структурно, привязано к наличию под-landmarks, задокументировано в SCHEMA.md. **PASS**
- **C3 (различимость body_lean, ключевой):** после фикса — глобально по present-кадрам (n=575): **std=0.239**, min=-0.246,
  max=0.935, **frac==1.0 = 0.0000**, 546 уникальных значений из 575. Per-video std 0.060–0.243 (было =1.0 константа = FAIL).
  Спот: `arm_openness` std 0.33–0.39 (не константа). **PASS**
- **C4 (aggregated):** 37–43 скалярных поля (≥24 ✓); значения варьируются между видео (не константа).
  NaN-поля только семейств engagement/confidence/early-late/ratios/pose-avg/mouth_rhythm — все зависят от малого числа
  опорных кадров ИЛИ отсутствующего тира. Доля NaN-полей: ok-видео 0–19% (70_Id 0%, smoke 0%, AGfPggrmw 4.7%, QfzWnPhdg3g 19%),
  empty-видео 23.7%. **Исключение:** ItRcDFKFiSU (status=ok) = 35% NaN (14/40), т.к. в ролике полностью отсутствует pose-тир
  → avg_arm_openness/pose_expansion/confidence/engagement легитимно NaN. Числовой порог ≤20% превышен только когда отсутствует
  целый тир landmarks; сами NaN структурны (by design), не случайны. **PASS с оговоркой** (порог 20% строг для degraded-роликов).

## Назначение выхода (Encoder-fit)
- `seq_*` [N,F] dense-seq с осью SoT + маска `landmarks_present`; для Encoder: `landmarks_present` как основная маска +
  per-feature `isfinite()` (тиры pose/mouth разрежены — ожидаемо на контенте без полного тела/лица).
- `aggregated` — 33–43 video-level скаляра для аналитиков/tabular head; варьируются между роликами.

## Вердикт (предлагаемый): ✅ ШТАМПОВАТЬ
Все универсальные гейты и C1–C3 — PASS; C4 PASS с задокументированной оговоркой (порог 20% строг только для роликов
с отсутствующим тиром landmarks, NaN by design). Ключевой дефект audit v4 (body_lean=константа 1.0) устранён.
Открытых блокеров нет.
