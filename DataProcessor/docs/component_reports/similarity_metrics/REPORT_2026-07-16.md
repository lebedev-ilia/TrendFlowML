# REPORT: similarity_metrics

**Дата:** 2026-07-16  
**Версия:** 2.0.2 · Схема: similarity_metrics_npz_v3  
**Артефакт:** similarity_metrics/results.npz  
**Авто-штамп при 100% PASS**

---

## Резюме

Компонент валидирован офлайн:
- 23 существующих NPZ из storage/result_store прошли батч-валидатор
- 6 синтетических тестов (golden, N=1, no-dep, mismatch, F-stability, no-optional-deps)
- GPU-прогон не проводился (раннера run_similarity_local.py нет; все гейты PASS на имеющихся данных)

---

## Универсальные гейты (U1–U6)

| Гейт | Результат | Доказательство |
|------|-----------|---------------|
| U1 | **PASS** | batch validate_similarity_metrics_npz.py: 23/23 OK (schema+struct+ranges) |
| U2 | **PASS** | ranges check: times_s монотонен на всех 23 NPZ |
| U3 | **PASS** | centroid_sims ∈ [-1,1], temporal_sim_next ∈ [-1,1]; все finite при status=ok |
| U4 | **PASS** | N=1 → temporal_sim_next len=0, temporal_sim_mean=NaN; no core_clip → FileNotFoundError |
| U5 | **PASS** | Golden max\|Δ\|=0.00e+00 (синтетика, чистый numpy, детерминирован) |
| U6 | **PASS** | N=1/10/43/100 → F=39, валидатор rc=0 |

---

## Критерии компонента (C1–C4)

| Критерий | Результат | Значения |
|----------|-----------|---------|
| C1: NaN by design | **PASS** | 24/39 NaN (все 23 NPZ) когда ref_present=False — задокументировано |
| C2: вариативность | **PASS** | centroid_sim_mean CV=4.8% (mean=0.963, std=0.046, min=0.830, max=0.985, N=23) |
| C3: F=39 stable | **PASS** | F=39 при N=1/10/43/100 |
| C4: когерентность finite | **PASS** | centroid_sims/temporal_sim_next/n_frames finite на всех ok-NPZ |

---

## Числа по ключевым полям (23 NPZ)

| Поле | Mean | Std | Min | Max |
|------|------|-----|-----|-----|
| N (frame_indices) | — | — | 12 | 119 |
| centroid_sim_mean | 0.963 | 0.046 | 0.830 | 0.985 |
| temporal_sim_mean | ~0.92..0.997 | — | — | — |
| NaN в feature_values | 24/39 | 0 | 24 | 24 |
| status=ok | 23/23 | — | — | — |

---

## Синтетические тесты

```
Тест 1: Golden max|Δ|=0.00e+00 → PASS
Тест 2: N=1 → tn.len=0, cs.len=1, cs[0]=1.0, temporal_sim_mean=NaN → PASS
Тест 3: нет core_clip → FileNotFoundError → PASS
Тест 4: frame_indices mismatch → RuntimeError → PASS
Тест 5: F=39 при N=1/10/43/100 → PASS
Тест 6: no optional deps → 24 NaN, ref_present=False → PASS
```

---

## Предупреждения (не блокирующие)

**WARNING: мёртвый код в similarity_metrics.py**  
Методы `compute_style_similarity`, `compute_text_similarity`, `compute_audio_similarity`, `compute_emotion_behavior_similarity`, `compute_temporal_similarity`, `compute_high_level_scores`, `compute_batch_metrics`, `extract_all` и блок `if __name__=='__main__'` — наследие от другой архитектуры. Используют незадекларированные `cosine`, `wasserstein_distance`, `pearsonr` (scipy) и `self.similarity_weights` (отсутствует). Не вызываются в production (`process()`, `run()`). Рекомендация: удалить или вынести в отдельный research-файл.

**WARNING: нет раннера run_similarity_local.py**  
GPU-прогон на /workspace/scene_videos не проводился. Компонент требует только CPU (numpy, cosine similarity), GPU нужен только для deps (core_clip, clap). Функциональность подтверждена на 23 реальных NPZ.

**WARNING: reference pack отсутствует**  
`dp_models/bundled_models/similarity/reference_sets/` пуст → reference_present=False во всех прогонах. 24/39 feature_values = NaN by design (uniqueness_*, reference_similarity_*). Когерентная часть (15/39 фичей) работает полностью.

---

## Вердикт

**ШТАМПОВАТЬ** (авто-штамп при 100% PASS U1–U6 + C1–C4)

Компонент корректен по core-логике (когерентность, ось времени, детерминизм, контракт NPZ). Reference-часть NaN by design — нет reference pack, это ожидаемо. Мёртвый код не влияет на production.
