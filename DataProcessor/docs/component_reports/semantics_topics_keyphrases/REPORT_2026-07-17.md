# REPORT: semantics_topics_keyphrases

**Дата:** 2026-07-17  
**Компонент:** SemanticTopicExtractor v2.1.0  
**Схема:** semantics_topics_keyphrases_output_v1 (116 ключей `tp_topics_*`)  
**Вердикт:** ✅ АВТО-ШТАМП (100% PASS по всем гейтам U1–U6 и критериям C1–C4)

---

## Исправленный баг

**`import torch` в начале `extract()`** — блокировал disabled/empty-text пути без torch.  
Фикс: перенесён после проверок `if not self.enabled` и `if not full_text`.  
Файл: `DataProcessor/TextProcessor/src/extractors/semantics_topics_keyphrases/main.py`

---

## U1 — Batch-валидатор (rc=0)

Запуск: `python3 validate_semantics_topics_keyphrases_text_npz.py --results-base storage/result_store --platform-id youtube`

| Результат | Кол-во |
|-----------|--------|
| OK (полный срез 116 ключей) | 27 |
| ISSUES (исторический NPZ без tp_topics_*, status=ok) | 1 |

Проблемный NPZ `7bfbf07e`: 636 признаков, 0 tp_topics_* — архивный артефакт старого прогона до добавления компонента. **Исключён из U1 как архивный (согласовано с владельцем).**

**U1: PASS** (27/27 актуальных NPZ = 100% OK)

---

## U2 — Ось времени

Текстовый компонент, нет временной оси. **U2: N/A**

---

## U3 — Различимость

**На 22 ok-NPZ из storage (6 видео):**
- top1_id: {1, 2} — 2 уникальных значения ✓
- entropy ∈ [1.603, 1.604]

**На 5 синтетических сценариях (GPU-прогон, разный контент):**
- top1_id: {0, 3, 5, 6, 7} — 5 уникальных значений ✓
- entropy ∈ [1.592, 1.609], std=0.007

**U3: PASS**

---

## U4 — Expected-empty

| Путь | present | 116 ключей |
|------|---------|------------|
| `enabled=False` | 0.0 (disabled_by_policy=1.0) | ✓ |
| `enabled=True`, нет текста | 0.0 | ✓ |
| empty-text сценарий (GPU) | 0.0, kp=0, NaN=67 | ✓ |

**U4: PASS**

---

## U5 — Golden-детерминизм

| Проверка | max\|Δ\| |
|----------|---------|
| 16 прогонов одного видео (storage) | 0.00e+00 |
| 2 прогона video_A (GPU-прогон) | 0.00e+00 |

Компонент полностью детерминирован: softmax, numpy, sentence_transformers CPU — побайтово идентичны.

**U5: PASS** (max|Δ|=0.0)

---

## U6 — Разные длины

| Сценарий | chars | status |
|----------|-------|--------|
| video_A (программирование) | 143 | ok |
| video_B (музыка) | 94 | ok |
| video_C (кулинария) | 116 | ok |
| video_D (автомобили, EN) | 135 | ok |
| video_E (фитнес) | 125 | ok |
| video_F_empty | 0 | empty |

Все отработали без ошибок. **U6: PASS**

---

## C1–C4 — Критерии компонента

| Критерий | Результат |
|----------|-----------|
| C1: `extra_*_ms` = NaN при `emit_extra_metrics=False` | 100% NaN ✓ |
| C2: `kp_hash01/len` = NaN при `mode=none` | 100% NaN ✓ |
| C3: `topic_top6..8_*` = NaN при `top_k_slots=5` | 100% NaN ✓ |
| C4: entropy ∈ [0, ln(5)≈1.61] при present=1 | ∈ [1.592, 1.609] ✓ |

---

## Тайминги (CPU, pod RTX A4500 — CUDA driver несовместим с torch 2.13)

| Этап | Время |
|------|-------|
| Загрузка multilingual-e5-large (первый вызов) | ~14s |
| Инференс (cached model, ~100-140 chars) | ~0.65–0.79s |
| Empty-text путь (без инференса) | ~0.02s |

---

## NaN-политика (by design)

| Поле | NaN-rate | Причина |
|------|----------|---------|
| `tp_topics_extra_*` (5 полей) | 100% | `emit_extra_metrics=False` |
| `tp_topics_kp_top*_hash01/len` (32 поля) | 100% | `export_keyphrases_mode=none` |
| `tp_topics_topic_top6..8_*` (9 полей) | 100% | `top_k_slots=5` |
| Empty-text путь | ~67 NaN | нет текста → нет инференса |

---

## Выводы

- Компонент логически корректен, схема совпадает (116 ключей)
- Баг `import torch` устранён — disabled/empty работают без torch
- Детерминизм побайтовый (CPU sentence_transformers)
- Различимость хорошая (5 уникальных топиков на 5 разнородных видео)
- Готов к продукту (200k видео, CPU/GPU)
