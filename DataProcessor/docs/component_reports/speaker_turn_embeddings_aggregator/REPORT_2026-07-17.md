# REPORT — speaker_turn_embeddings_aggregator

**Дата:** 2026-07-17  
**Версия:** 1.3.0  
**Схема:** speaker_turn_embeddings_aggregator_output_v1 (17 ключей tp_spkemb_*)  
**Валидация:** синтетическая (CPU-only компонент, нет GPU-прогонов)  
**Штамп:** ✅ авто-штамп при 100% PASS  
**Основание:** синтетическая валидация полна (21/21), batch-валидатор 28/28 OK, реальные diar+ASR в корпусе отсутствуют (expected для MVP).

---

## Окружение

- Python: tp_venv (sentence-transformers 5.2.2, torch 2.12.1+cu126, numpy 2.0.2)
- Модель: intfloat/multilingual-e5-large через dp_models (bundled_models), init 7.1с
- weights_digest: cf2a06cd38e7... (первые 12 символов)
- Под: не поднимался (CPU-only)

---

## U1 — Batch-валидатор

```
python3 validate_speaker_turn_embeddings_aggregator_text_npz.py --results-base storage/result_store --platform-id youtube
Проверено файлов: 28
28/28 [OK]
```
rc=0, все 28 NPZ → OK. Все status=ok, tp_spkemb_present=0.0, NaN=5/17 (extra-ключи by design).

## U2 — Ось времени
N/A — TextProcessor, нет frame_indices/union_timestamps_sec.

## U3 — Различимость

```
cos_sim(Alice mean, Bob mean) = 0.9236  < 0.999  ✅
```
Эмбеддинги разных спикеров различимы.

## U4 — Expected-empty

```python
doc_empty = make_doc(speakers={})
r = ext.extract(doc_empty)  # require_input=False
→ tp_spkemb_present=0.0, no crash  ✅
```

## U5 — Golden детерминизм

| Тест | max|Δ| features_flat | max|Δ| npy |
|------|----------------------|-----------|
| legacy (2 прогона) | 0.0 | 0.0 (4 файла) |
| diar+ASR (2 прогона) | 0.0 | — |

CPU sentence-transformers побайтово детерминирован (без autocast/fp16).

## U6 — Разные длины

| Тест | Тёрнов | Спикеров | present |
|------|--------|----------|---------|
| legacy 1 спикер 1 тёрн | 1 | 1 | 1 |
| legacy 2 спикера 3 тёрна | 3 | 2 | 1 |
| diar+ASR 2 спикера 3 тёрна | 3 | 2 | 1 |
| empty | 0 | 0 | 0 |

---

## C1 — extra-ключи NaN/finite

emit_extra_metrics=True: все 5 extra-ключей finite ✅  
emit_extra_metrics=False: все 5 extra-ключей NaN ✅  
В 28 NPZ storage (production default): NaN=5/17 ✅

## C2 — Счётчики монотонны

speakers_embedded ≤ speakers_total на всех тестах ✅  
present=1 → embedded≥1 ✅

## C3 — Флаги взаимоисключающие

legacy: mode_diar=0, mode_legacy=1, asr=0, diar=0 ✅  
diar+ASR: mode_diar=1, mode_legacy=0, asr=1, diar=1 ✅  
empty: все 0 ✅

## C4 — require_input

require_input=True + пустой doc → RuntimeError("…required input is missing…") ✅  
require_input=False + пустой doc → valid empty, no exception ✅

---

## T8 — Артефакты .npy

4 файла (spk000_mean, spk000_max, spk001_mean, spk001_max):  
- все finite ✅  
- L2-норма ≈ 1.0 (||v|| ≈ 1.0) ✅  
- атомарная запись (.tmp.npy → replace) ✅

---

## Итог по гейтам

| Гейт | Статус |
|------|--------|
| U1 batch-валидатор 28/28 | ✅ PASS |
| U2 ось времени | N/A |
| U3 различимость cos<0.99 | ✅ PASS |
| U4 expected-empty | ✅ PASS |
| U5 golden 0.0 | ✅ PASS |
| U6 разные длины | ✅ PASS |
| C1 extra NaN/finite | ✅ PASS |
| C2 счётчики монотонны | ✅ PASS |
| C3 флаги XOR | ✅ PASS |
| C4 require_input | ✅ PASS |

**PASS 10/10. Авто-штамп ✅**

---

## Рекомендации

1. При появлении реального diar+ASR pipeline — перепрогнать на реальных NPZ для проверки happy-path в production
2. Производительность: init 7.1с/процесс (torch.hub + ST load) — в проде разовая стоимость воркера
3. GPU fp16 — поддерживается, не тестировался (в MVP CPU достаточен)
