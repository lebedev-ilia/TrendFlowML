# Отчёт: scene_classification — сессия 2026-07-05 (в работе 🔄)

Автор: Claude (автономная часовая сессия). GPU-под RunPod RTX A4500. Static-review — `RUN_SPEC.md`.

## Сделано (логика — приоритет)
1. **🔴 Scene-эмбеддинг для Models (главная model-fit правка).** Добавлен per-frame плотный токен
   `frame_scene_embedding (N, D)` L2 — **переиспользует core_clip (CLIP) эмбеддинг кадра** (уже
   загружен как hard-dep → ноль лишней инференс-стоимости). Аналог penultimate у action_recognition:
   Моделям — богатое представление сцены сверх 365-классового распределения. Источник/размерность в
   `scene_embedding_source`/`scene_embedding_dim`.
2. **Backbone.** A (Places365 ResNet50, дефолт) + B (CLIP zero-shot, `label_fusion=clip`) — рабочие.
   Добавлен alias **resnet152** (крупнее/качественнее). C (ConvNeXt/ViT под Places365): **публичных
   fine-tuned весов нет** (ресёрч: официальные — только ResNet/VGG16-hybrid CSAIL) → «современный» путь =
   CLIP zero-shot (B), он же даёт эмбеддинг. Fine-tune ViT под Places365 — как будущая опция.
3. **Входной валидатор** `validate_scene_classification_input.py`: core_clip (+places365_text для clip),
   cut_detection (shot boundaries), subset `scene.frame_indices ⊆ core_clip.frame_indices`, run-identity.

## Валидация модели на GPU (реально прогнано мной)
- Places365 ResNet50 (веса CSAIL) грузится чисто (missing 0 / unexpected 0).
- **Бенчмарк (fp16, bs=64): 2188 img/s (×1.7 к fp32), пик VRAM 812 МБ** (детали — ledger).
- **Корректность на реальных HF-видео** (top-3 сцены): `music_studio`, `ice_cream_parlor/butchers_shop`,
  `arena/performance` — правдоподобно (аналог «кларнет»-проверки AR). Модель осмысленна.

## Валидация модель-слоя КОМПОНЕНТА на GPU (через ModelManager, реально прогнано)
- `mm.get("places365_resnet50")` → inprocess/torch, **resolve+load 2.3 c**, forward (8,365) ✅.
  Это настоящий путь компонента (factory `create_torchvision_resnet`, strip_prefix `module.`,
  categories_relpath) — не только сырой бенчмарк. Inprocess Places365 полностью рабочий.
- **Находка (deploy):** backbone «C» — specs `places365_timm_{convnext_tiny/small/base, vit_base/large,
  efficientnet_b0..b3, resnet101}` **уже есть** в spec_catalog + timm-factory. Не хватает только
  **Places365-fine-tuned весов** (публичных нет). Т.е. добавление ConvNeXt/ViT — вопрос весов, не кода.
- **Синк-фикс:** `dp_models/factories/` не долетал на под при частичном rsync → досинхронизирован
  (нужен для любого inprocess-провайдера; для bootstrap уже в репо).

## Оптимизация (вывод)
Канонично **inprocess ResNet50 + fp16 + batch 32–64** (Triton batch=1 = узкое место, L1/HTTP 400).
Рекомендация: дефолт inprocess+fp16+batch; Triton — опционально.

## Не сделано в эту сессию (следующая итерация)
- **Полный прогон цепочки** `Segmenter→core_clip→cut_detection→scene` на 15 видео: core_clip работает
  **только через Triton** (CLIP ONNX) → нужен standup Triton (+ cut_detection). Видео (328 mp4) уже на
  поде. Отсюда — незакрытые пункты: сравнение `label_fusion` places vs clip (Q4), качество сегментации
  по сценам (cut_detection границы + min_scene_seconds), проверка advanced-семантики (эстетика/атмосфера),
  golden, метрики цепочки в ledger.
- fp16/batch-дефолты прошить в конфиг компонента (сейчас проверено на модели, не в проде-обвязке).

## ПОЛНАЯ ЦЕПОЧКА провалидирована на GPU (2026-07-05, БЕЗ Triton)
Ключевая находка: **Triton НЕ нужен.** core_clip умеет inprocess (`clip.load ViT-B/32`),
cut_detection — `--no-use-clip --no-require-core-optical-flow` (внутренний farneback). Единственный
Triton-потребитель (core_optical_flow RAFT) для scene не обязателен. Прямой вызов компонентов
(обход dep-проверки оркестратора) — раннер `scripts/run_scene_local.py`.

**Прогон end-to-end (Segmenter→core_clip→cut_detection→scene), все стадии rc=0:**
- тайминги: 37+14+31+13 ≈ **95 c** (детали — ledger);
- выход: 112 кадров, 4 сцены, **`frame_scene_embedding (112,512)` из core_clip** ✅, метки осмысленны
  (beauty_salon/hospital_room/laundromat);
- **оба валидатора (вход+выход) pass**; JSON-схема обновлена (+3 ключа scene-эмбеддинга);
- **label_fusion places vs clip**: одинаковые метки на этом видео (согласованно).

Правки по ходу прогона: JSON-схема `schemas/scene_classification_npz_v2.json` (+ scene-эмбеддинг-ключи);
input-валидатор (cut_detection хранит границы в `detections`, не топ-`shot_bound`); librosa/ffmpeg на под.

## Статус
✅ **логика + полная цепочка провалидированы на GPU** (inprocess, без Triton). Scene-эмбеддинг,
backbone, сегментация, оба label_fusion, валидаторы — работают, метки осмысленны. Прод-готово по логике.
Остаток (не блокер): прогон на всех 15+ видео для статистики (сделан 1 + сравнение fusion); jump-cut
качество cut_detection (нужны core_face_landmarks/core_object_detections — опц.).

## Следующие шаги (для след. сессии)
> **Уточнение (важно): Triton, вероятно, НЕ нужен.** core_clip умеет **inprocess** (`clip.load ViT-B/32`),
> cut_detection — `no_use_clip=true`. Единственный Triton-потребитель штатного профиля —
> `core_optical_flow (raft_triton)`, который для scene не обязателен. Подготовлен trimmed-профиль
> `configs/audit_v3/visual/scene_chain_inprocess.yaml` (optical_flow off → cut_detection farneback).

1. Поднять под (сейчас RunPod: «not enough free GPUs» — передеплой/повтор Start).
2. **Путь A (пробовать первым, без Triton):** Segmenter(scene профиль) → visual `--cfg-path
   scene_chain_inprocess.yaml` (core_clip inprocess + cut_detection farneback + scene inprocess) на
   15 HF-видео. cut_detection — с `--no-require-core-optical-flow`.
3. **Путь B (fallback):** если farneback-путь не заведётся из оркестратора — включить core_optical_flow
   (triton raft) + `backend/scripts/e2e_triton_docker.sh up` (Triton), профиль `visual_scene_classification_only.yaml`.
4. Сравнить `label_fusion` places vs clip (два прогона). REPORT-полный (4 оси) + метрики + ledger-тайминги → штамп.
5. Провижн inprocess Places365 в bootstrap (веса CSAIL авто-скачиваются; фабрика `create_torchvision_resnet` готова).
