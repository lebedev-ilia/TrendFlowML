model_name: semantic_clusters_v1
model_version: "v1"
role: text_asset
runtime: inprocess
engine: files
precision: fp32
device_policy: cpu
weights_digest: auto
local_artifacts:
  - path: text/semantic_clusters_v1/pca.npy
    kind: file
  - path: text/semantic_clusters_v1/centroids.npy
    kind: file
  - path: text/semantic_clusters_v1/clusters.jsonl
    kind: file
runtime_params:
  # NOTE: These are assets (not an ML model). The extractor uses ModelManager.resolve(spec)
  # to validate existence and to compute a deterministic weights_digest for reproducibility.
  pca_relpath: text/semantic_clusters_v1/pca.npy
  centroids_relpath: text/semantic_clusters_v1/centroids.npy
  clusters_jsonl_relpath: text/semantic_clusters_v1/clusters.jsonl
  cluster_db_version: v1


